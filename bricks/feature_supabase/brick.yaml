name: feature
description: Flutter Clean-Arch Feature scaffold (models + data_sources impl + DI)
version: 0.1.0+1
environment:
  mason: ^0.1.0

vars:
  feature_name:
    type: string
    prompt: "생성할 Feature 이름을 입력하세요 (예: user)"

#hooks:
#  post_gen:
#    - bash: hooks/register_di.sh