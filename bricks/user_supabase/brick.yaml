name: user_supabase
description: A user profile feature for Supabase backend following Clean Architecture
version: 0.1.0+1

vars:
  feature_name:
    type: string
    description: The name of the feature
    default: user
    prompt: What is the feature name?